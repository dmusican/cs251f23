#include <stdlib.h>

struct LinkedList {
    int value;
    struct LinkedList *next;
};

typedef struct LinkedList Potato;


int main() {
    Potato *list = NULL;
    list.insertFrontLL(list, 12);

    // print list
}
